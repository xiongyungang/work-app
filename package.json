{
  "name": "worker-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply worker-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply worker-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250508.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
